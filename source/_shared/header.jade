header.PageHeader(role='banner')
  .Section
    h1.Logo
      a(href='/') Logo

    //- TODO make this a helper
    - nav = [{name: 'Home', path: '/'}, {name: 'Other', path: '/other/path'}]
    nav.Nav(role='navigation')
      ul
        - each page in nav
          li
            - if (current !== page.path)
              a(href=page.path)= page.name
            - else
              span.active= page.name
